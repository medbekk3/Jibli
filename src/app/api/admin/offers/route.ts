import{FieldValue}from"firebase-admin/firestore";import{NextRequest}from"next/server";import{getAdminDb}from"@/lib/firebase/admin";import{adminFailure,adminSuccess,logAdminApiFailure}from"@/lib/firebase/admin-response";import{isAdminSessionError,requireActiveAdminSession}from"@/lib/firebase/admin-session";import{serializeDocument}from"@/lib/firebase/serialize-firestore";
export async function GET(){try{await requireActiveAdminSession();const s=await getAdminDb().collection("offers").get();return adminSuccess(s.docs.map(d=>serializeDocument(d.id,d.data())))}catch(e){logAdminApiFailure("GET /api/admin/offers","القراءة",e);if(isAdminSessionError(e))return adminFailure(e.code,e.message,e.status);return adminFailure("ADMIN_OPERATION_FAILED","تعذر تحميل العروض.",500)}}
export async function POST(r:NextRequest){try{await requireActiveAdminSession();const v=await r.json();if(!String(v.title??"").trim())return adminFailure("INVALID_FORM_DATA","عنوان العرض مطلوب.",400);const clean=Object.fromEntries(Object.entries(v).filter(([,x])=>x!==undefined));const ref=getAdminDb().collection("offers").doc();await ref.set({...clean,createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()});return adminSuccess({id:ref.id},201)}catch(e){logAdminApiFailure("POST /api/admin/offers","الإنشاء",e);if(isAdminSessionError(e))return adminFailure(e.code,e.message,e.status);return adminFailure("ADMIN_OPERATION_FAILED","تعذر إضافة العرض.",500)}}
