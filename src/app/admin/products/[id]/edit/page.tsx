"use client"; import { useParams } from "next/navigation"; import { useEffect,useState } from "react"; import { AdminShell } from "@/components/admin/admin-shell"; import { AdminLoadingSkeleton, AdminPageHeader } from "@/components/admin/admin-ui"; import { ProductForm } from "@/features/admin/product-form"; import { getAdminProduct as getProduct } from "@/lib/firebase/services/admin-product.service"; import type { ProductDocument } from "@/types/collections";
export default function EditProduct(){const id=String(useParams<{id:string}>().id);const [item,setItem]=useState<ProductDocument|null>(null);useEffect(()=>{getProduct(id).then(setItem)},[id]);return <AdminShell title="تعديل الأكلة"><AdminPageHeader title="تعديل الأكلة" breadcrumbs={[{label:"الأكلات",href:"/admin/products"},{label:"تعديل"}]}/>{item?<ProductForm restaurantId={item.restaurantId} productId={id}/>:<AdminLoadingSkeleton/>}</AdminShell>}
