# Notification sounds

Optional original/licensed files:
- new-order.mp3
- order-update.mp3

The application uses a Web Audio fallback when these files are absent. Background notification sound is controlled by the browser and operating system.